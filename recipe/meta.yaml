{% set version = "2.2.0" %}
{% set sha256 = "2feed455b5b2184d6a174358a6d2d7c8bb52f59802941808ca5a81678fe9279f" %}
{% set name = "depfinder" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: python -m pip install --no-deps --ignore-installed .
  entry_points:
    - depfinder = depfinder.cli:cli

requirements:
  host:
    - pip
    - python <3.7
    - setuptools
  run:
    # stdlib-list is not yet py37 compatible https://github.com/ericdill/depfinder/issues/20
    - python <3.7
    - stdlib-list
    - pyyaml

test:
  commands:
    - depfinder -h
  imports:
    - depfinder

about:
  home: http://github.com/ericdill/depfinder
  license: BSD 3-Clause
  license_file: LICENSE
  summary: Find all the unique imports in your library

extra:
  recipe-maintainers:
    - ericdill
    - mariusvniekerk
    - tonyfast
    - ocefpaf
